<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- DEV ПРОФИЛЬ: Вывод в консоль с подсветкой -->
    <springProfile name="dev, default">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{yyyy-MM-dd HH:mm:ss} %highlight(%-5level) [%thread] %cyan([traceId=%X{traceId}]) %logger{36} - %msg%n</pattern>
            </encoder>
        </appender>

        <root level="INFO">
            <appender-ref ref="CONSOLE" />
        </root>
    </springProfile>

    <!-- PROD ПРОФИЛЬ: Запись в файл с ротацией -->
    <springProfile name="prod">
        <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>logs/application.log</file>

            <!-- Добавлен енкодер для форматирования строк в файле -->
            <encoder>
                <pattern>%d{yyyy-MM-dd HH:mm:ss} | %-5level | [%X{traceId}] | [%X{httpReq}] | user=%X{user} | %logger{20} : %msg%n</pattern>
            </encoder>

            <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
                <fileNamePattern>logs/archived/app-%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
                <maxFileSize>10MB</maxFileSize>
                <maxHistory>30</maxHistory>
                <totalSizeCap>3GB</totalSizeCap>
            </rollingPolicy>
        </appender>

        <!-- Глушим спам фреймворков -->
        <logger name="org.springframework" level="WARN"/>
        <logger name="org.hibernate" level="WARN"/>
        <logger name="org.apache.catalina" level="WARN"/>
        <logger name="org.flywaydb" level="INFO"/>

        <!-- Наше приложение логируем подробно -->
        <logger name="MeetingRoom_BookingSystem" level="INFO"/>

        <root level="INFO">
            <appender-ref ref="FILE" />
        </root>
    </springProfile>

</configuration>