spring:
  datasource:
    url: jdbc:mysql://localhost:3306/booking_jwt?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: workbench_user
    password: 090994ss

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    baseline-on-migrate: true

  profiles:
    active: dev

  output:
    ansi:
      enabled: ALWAYS

app:
  jwt:
    secret: ${JWT_SECRET:Super_Mega_Secret_Key_Named_Zarifa_Because_This_Key_Is_Always_Stored_In_My_Heart_<3_256bits}

